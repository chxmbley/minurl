# This file is provided for convenience. In a real-world application, this would not be included in the repository and
# would instead be described in setup instructions.

NODE_ENV=test
DATABASE_URL="file:./test.db"
